# SSH

1. Q：想要通过 Vscode 免密登录远程服务器，以在本地配置好公钥、私钥，并已把公钥发送到服务器上并生成 authorized_keys，为什么在 Vscode 上登录服务器时，仍要求输入密码
1. A：
  1.1 将服务器上的 .ssh 文件的权限修改为 700，.ssh 下的 authorized_keys 文件权限修改为 600；
  1.2 将本地 .ssh 下的 id_rsa 文件权限修改为仅为本机用户，且权限为“读取和执行、读取”（这一步不一定是必需的）
